spring:
  datasource:
    url: jdbc:mysql://localhost:3306/bigdata?characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    #    change the password to your own mysql password
    password: password
  jpa:
    hibernate:
      ddl-auto: update

  server:
    port:8080